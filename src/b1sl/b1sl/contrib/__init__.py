"""
b1sl.b1sl.contrib
~~~~~~~~~~~~~~~~~~~~~~~~
Framework-specific extensions for the b1sl SDK.

The core SDK (b1sl.b1sl) is fully framework-agnostic.
This ``contrib`` namespace contains optional integrations for specific web
frameworks. They are NOT imported by the core SDK and carry no extra
dependencies unless you use them.

Currently available:

    b1sl.b1sl.contrib.django  — Django middleware, app configs, etc.
"""
